<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jaekapps.expensetracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup">
        <activity
            android:name=".view.activities.OnBoardingScreenActivity"
            android:theme="@style/AppTheme.OnBoardingScreenActivityTheme">

        </activity>
        <activity android:name=".view.activities.ItemDetailsActivity" />
        <activity android:name=".view.activities.ItemsActivity" />
        <activity android:name=".view.activities.SignInOrSignUpActivity" />
        <activity android:name=".view.activities.AddItemCategoryActivity" />
        <activity android:name=".view.activities.ExpenseIncomeActivity" />
        <activity
            android:name=".view.activities.SplashScreenActivity"
            android:theme="@style/AppTheme.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activities.HomeScreenActivity"
            android:theme="@style/AppTheme.HomeScreenActivity" />
        <activity
            android:name=".view.activities.MainActivity"
            android:theme="@style/AppTheme.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>